<div class="container">
<div class="row card">
<div class="col">   
  <button onclick="showAndHide('commentIdA<%=comment.id%>')" class="btn btn-secondary">  <strong>comment:</strong><%= comment.body %></button>
</div>

<div class="col">
</div>
<div class="col">
  <div id="commentIdA<%=comment.id%>" style="display:None">
  <strong>commented_By:</strong>
  <%= User.getName(comment.commenter) %>
  <strong>commented_On:</strong>
  <%= comment.created_at.in_time_zone("Asia/Calcutta").strftime('%a %b %d %H:%M:%S %Z %Y') %> <strong>Last_Edited_on:</strong>
  
  <strong>Last_Edited_on:</strong>
  <%= comment.updated_at.in_time_zone("Asia/Calcutta").strftime('%a %b %d %H:%M:%S %Z %Y') %>
  <% if comment.commenter.to_i == current_user.id %>
      
  <%= link_to 'Edit Comment', edit_question_answer_comment_path(@question,comment.answer,comment) %>
  <br>
  <%= link_to 'Delete comment', [@question,comment.answer, comment],
            method: :delete,
            data: { confirm: 'Are you sure?' } %>

<%end%>
  </div>
</div>
</div>
</div>




