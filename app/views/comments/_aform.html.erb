<%= form_with(model: [ @answer.question,@answer, @answer.comments.build ], local: true) do |form| %>
  <p>
    <%= form.label "Your comment on the above answer:" %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit "Submit Comment", class: "button" %>
  </p>
<% end %>