<h1>Edit comment</h1>
    <%= form_for ([@comment.answer.question,@comment.answer,@comment]) do |form| %>
    
    <% if @comment.errors.any? %>
        <div id="error_explanation">
        <h2>  
            <%= pluralize(@comment.errors.count, "error") %> prohibited
            this comment from being saved:
        </h2>
        <ul>
            <% @comment.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
            <% end %>
        </ul>
        </div>
    <% end %>
   
  <div class="form-group">
    <%= form.label "Your comment on the above answer:" %><br>
    <%= form.text_area :body , class: 'form-control' , :required => 'required' ,  placeholder: "Write your comment"%>
  </div>

  <%= form.submit "Submit Comment", class: "btn btn-primary" %>  

    <% end %>
    
    <%= link_to 'Back', question_path(@comment.answer.question) %>
