<%= form_with(model: [ @question, @question.answers.build ], local: true) do |form| %>
  <p>
    <%= form.label :posted_by %><br>
    <%= form.text_field :posted_by %>
  </p>
  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>